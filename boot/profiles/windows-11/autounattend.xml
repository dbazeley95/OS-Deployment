<?xml version="1.0" encoding="utf-8"?>
<!--
  Skeletal unattend.xml for a Windows 11 unattended install.
  This is a starting point, not a complete answer file - Windows unattend
  configs are dense; fill in the passes you need (WindowsPE, specialize,
  oobeSystem) following Microsoft's Unattend reference:
  https://learn.microsoft.com/windows-hardware/customize/desktop/unattend/

  Notes for this scaffold:
  - Windows ISOs are NOT redistributable. Upload your own licensed media to
    R2 under windows-11/ (see boot/profiles/windows-11/README.md).
  - PXE-booting Windows Setup needs bootx64.efi + boot.sdi from the install
    media's \efi\microsoft\boot and \sources directories, plus the WIM
    image itself - the OS_PROFILES.windows-11 kernel/initrd paths in
    worker/src/lib/profiles.ts point at those two files.
  - Phone-home on Windows happens via a FirstLogonCommands entry running a
    PowerShell Invoke-RestMethod PATCH to /api/jobs/by-mac/<mac>, since
    late-commands don't exist here the way they do in cloud-init/preseed.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend">
  <settings pass="windowsPE">
    <component name="Microsoft-Windows-International-Core-WinPE" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <UILanguage>en-US</UILanguage>
    </component>
  </settings>

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <ComputerName>WIN-REIMAGED</ComputerName>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <ProtectYourPC>3</ProtectYourPC>
      </OOBE>
      <FirstLogonCommands>
        <SynchronousCommand>
          <Order>1</Order>
          <CommandLine>powershell -NoProfile -Command "$mac=(Get-NetAdapter | Select-Object -First 1 -ExpandProperty MacAddress).ToLower().Replace('-',':'); Invoke-RestMethod -Method Patch -Uri (\"https://REPLACE_WORKER_ORIGIN/api/jobs/by-mac/$mac\") -ContentType 'application/json' -Body '{\"status\":\"complete\"}'"</CommandLine>
          <Description>Phone home to os-deployment-worker</Description>
        </SynchronousCommand>
      </FirstLogonCommands>
    </component>
  </settings>
</unattend>
